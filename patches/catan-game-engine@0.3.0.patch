diff --git a/dist/index.js b/dist/index.js
index db59b1b1ee892ae2eba5001365086f0a7365879d..e6e0e2cc905b6c40290114132e6b7fb3cfd1b2dd 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -552,11 +552,11 @@ function getCanonicalEdgeId(v1, v2) {
 function findVertexForPort(vertices, _port, legacyVertexId) {
   const match = legacyVertexId.match(/(-?\d+)_(-?\d+)_v(\d+)/);
   if (match) {
-    const [, q, r, dir] = match;
-    const direction = parseInt(dir);
+    const q = Number(match[1]);
+    const r = Number(match[2]);
     const directionMap = ["N", "NE", "SE", "S", "SW", "NW"];
-    const directionName = directionMap[direction];
-    const canonicalId = `v_${q}_${r}_${directionName}`;
+    const direction = directionMap[Number(match[3])];
+    const canonicalId = getCanonicalVertexId({ q, r, s: -q - r, direction });
     return vertices.get(canonicalId) || null;
   }
   return null;
@@ -916,7 +916,8 @@ function rollDice(state) {
     throw new Error("Dice already rolled this turn");
   }
   const newState = cloneGameState(state);
-  const random = new SeededRandom(Date.now());
+  newState.rngDraws = (state.rngDraws ?? 0) + 1;
+      const random = new SeededRandom(`${state.config.randomSeed}:${newState.rngDraws}`);
   const dice = random.rollDice();
   const total = dice[0] + dice[1];
   newState.turn.diceRoll = dice;
@@ -1077,7 +1078,8 @@ function playKnight(state, playerId, newRobberHexId, targetPlayerId) {
       }
     });
     if (availableResources.length > 0) {
-      const random = new SeededRandom(Date.now());
+      newState.rngDraws = (state.rngDraws ?? 0) + 1;
+      const random = new SeededRandom(`${state.config.randomSeed}:${newState.rngDraws}`);
       const stolenResource = random.choice(availableResources);
       targetPlayer.resources[stolenResource]--;
       player.resources[stolenResource]++;
@@ -1183,7 +1185,8 @@ function moveRobber(state, playerId, hexId, targetPlayerId) {
         }
       });
       if (availableResources.length > 0) {
-        const random = new SeededRandom(Date.now());
+        newState.rngDraws = (state.rngDraws ?? 0) + 1;
+      const random = new SeededRandom(`${state.config.randomSeed}:${newState.rngDraws}`);
         const stolenResource = random.choice(availableResources);
         targetPlayer.resources[stolenResource]--;
         player.resources[stolenResource]++;
diff --git a/dist/index.mjs b/dist/index.mjs
index 011d486e622ab12134684f319107a05e1e7d13c4..eb98db2ecb220cba7665e1f43ee5428ce1f54038 100644
--- a/dist/index.mjs
+++ b/dist/index.mjs
@@ -497,11 +497,11 @@ function getCanonicalEdgeId(v1, v2) {
 function findVertexForPort(vertices, _port, legacyVertexId) {
   const match = legacyVertexId.match(/(-?\d+)_(-?\d+)_v(\d+)/);
   if (match) {
-    const [, q, r, dir] = match;
-    const direction = parseInt(dir);
+    const q = Number(match[1]);
+    const r = Number(match[2]);
     const directionMap = ["N", "NE", "SE", "S", "SW", "NW"];
-    const directionName = directionMap[direction];
-    const canonicalId = `v_${q}_${r}_${directionName}`;
+    const direction = directionMap[Number(match[3])];
+    const canonicalId = getCanonicalVertexId({ q, r, s: -q - r, direction });
     return vertices.get(canonicalId) || null;
   }
   return null;
@@ -861,7 +861,8 @@ function rollDice(state) {
     throw new Error("Dice already rolled this turn");
   }
   const newState = cloneGameState(state);
-  const random = new SeededRandom(Date.now());
+  newState.rngDraws = (state.rngDraws ?? 0) + 1;
+      const random = new SeededRandom(`${state.config.randomSeed}:${newState.rngDraws}`);
   const dice = random.rollDice();
   const total = dice[0] + dice[1];
   newState.turn.diceRoll = dice;
@@ -1022,7 +1023,8 @@ function playKnight(state, playerId, newRobberHexId, targetPlayerId) {
       }
     });
     if (availableResources.length > 0) {
-      const random = new SeededRandom(Date.now());
+      newState.rngDraws = (state.rngDraws ?? 0) + 1;
+      const random = new SeededRandom(`${state.config.randomSeed}:${newState.rngDraws}`);
       const stolenResource = random.choice(availableResources);
       targetPlayer.resources[stolenResource]--;
       player.resources[stolenResource]++;
@@ -1128,7 +1130,8 @@ function moveRobber(state, playerId, hexId, targetPlayerId) {
         }
       });
       if (availableResources.length > 0) {
-        const random = new SeededRandom(Date.now());
+        newState.rngDraws = (state.rngDraws ?? 0) + 1;
+      const random = new SeededRandom(`${state.config.randomSeed}:${newState.rngDraws}`);
         const stolenResource = random.choice(availableResources);
         targetPlayer.resources[stolenResource]--;
         player.resources[stolenResource]++;
